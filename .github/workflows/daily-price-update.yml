name: Daily Price Update

# Déclencheurs du workflow
on:
  # Exécution programmée : tous les jours à 8h UTC (10h Paris en hiver, 9h en été)
  schedule:
    - cron: "0 8 * * *" # Format: minute heure jour mois jour_semaine

  # Permet aussi le déclenchement manuel depuis l'interface GitHub
  workflow_dispatch:

jobs:
  update-prices:
    runs-on: ubuntu-latest

    steps:
      # Étape 1: Information sur l'exécution
      - name: Print execution time
        run: |
          echo "🕐 Updating prices at $(date)"
          echo "🌍 Timezone: UTC"
          echo "📍 Paris time: $(TZ=Europe/Paris date)"

      # Étape 2: Appel de la fonction Supabase Edge avec Service Role Key
      - name: Call Supabase Edge Function
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🔄 Calling update-prices function with Service Role authentication..."

          # Appel de la fonction Edge pour mettre à jour les prix (tous les utilisateurs)
          RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" -X POST \
            "${SUPABASE_URL}/functions/v1/update-prices" \
            -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
            -H "Content-Type: application/json")

          # Extraction du code HTTP et du corps de la réponse
          HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS" | cut -d: -f2)
          BODY=$(echo "$RESPONSE" | sed '/HTTP_STATUS/d')

          echo "📊 Response status: $HTTP_STATUS"
          echo "📋 Response body: $BODY"

          # Vérification du succès
          if [ "$HTTP_STATUS" -eq 200 ]; then
            echo "✅ Prices updated successfully!"
            
            # Extraction des statistiques (si disponibles)
            UPDATED=$(echo $BODY | grep -o '"updated":[0-9]*' | cut -d: -f2)
            FAILED=$(echo $BODY | grep -o '"failed":[0-9]*' | cut -d: -f2)
            
            if [ -n "$UPDATED" ]; then
              echo "✓ Updated: $UPDATED asset(s)"
            fi
            
            if [ -n "$FAILED" ] && [ "$FAILED" -gt 0 ]; then
              echo "⚠️ Failed: $FAILED asset(s)"
            fi
          else
            echo "❌ Price update failed with status $HTTP_STATUS"
            exit 1
          fi

      # Étape 3: Notification de succès
      - name: Success notification
        if: success()
        run: |
          echo "🎉 Daily price update completed successfully!"
          echo "📅 Next update scheduled for tomorrow at 8:00 UTC"

      # Étape 4: Notification d'échec
      - name: Failure notification
        if: failure()
        run: |
          echo "💥 Daily price update failed!"
          echo "🔍 Check the logs above for details"
          echo "⚠️ You may need to update prices manually today"
